{
  "name": "orieken-faker-suite-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "playground"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf packages/*/dist",
    "test": "npm -ws run test"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0"
  }
}