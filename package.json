{
  "name": "orieken-faker-suite-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "playground"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf packages/*/dist",
    "test": "npm -ws run test"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.6.1",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
